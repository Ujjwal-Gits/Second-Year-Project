# Audit level set to critical only — remaining moderate/high vulnerabilities
# are inside Prisma 7's internal CLI tooling (not runtime code) and have no
# upstream fix available without downgrading Prisma.
audit-level=critical
